#include <list>
using namespace std;
int main(){
  list<int> l;
  l.size();
  l.begin();
  l.end();
  l.push_front(x);
  l.push_back(x);
  l.pop_front();
  l.pop_back();
  l.insert(p,x);
  l.erase();
  l.clear();
  return 0;
}
