#include <iostream>
#include <stack>
using namespace std;
int mian(){
  stack<int> S;
  S.size()//返回栈的元素数
  S.top()//返回栈顶元素
  S.pop()//从栈中取出并删除元素
  S.push(3)//向栈中添加元素x
  S.empty()//在栈为空时返回true
  //时间复杂度均为O(1)
}
